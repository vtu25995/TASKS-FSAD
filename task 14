@Entity
public class Product {
    @Id @GeneratedValue
    private Long id;
    private String name;
    private double price;
    // getters/setters
}
@RestController
@RequestMapping("/products")
public class ProductController {
    @GetMapping
    public List<Product> getAll() { /* return product list */ }

    @GetMapping("/{id}")
    public Product getById(@PathVariable Long id) { /* return product */ }
}
